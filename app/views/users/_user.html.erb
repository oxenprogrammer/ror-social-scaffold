<li>
  Name: <%= user.name %>
  <span class="profile-link">
    <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
  </span>
  <span>
    <%= friendship_button(current_user, user) %>
    <%= friendship_update_button(current_user, user) %>
    <%= friend_button(current_user, user) %>
  </span>
</li>